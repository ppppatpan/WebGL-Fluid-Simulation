ROS subscriber version:

needs rosbridge to be launched (in other terminal):

Install:

$ sudo apt install ros-<distro>-rosbridge-suite

Launch:

$ roslaunch rosbridge_server rosbridge_websocket.launch
